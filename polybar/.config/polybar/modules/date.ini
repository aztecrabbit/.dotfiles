[module/date]

type = internal/date
interval = 1.0

date = "%B %d %Y, "
time = "%H:%M:%S "

label = %date%%time%

format = <label>
format-prefix = ${variables.format-right-prefix}
format-suffix = ${variables.format-right-suffix}
format-foreground = #EEFFFFFF
