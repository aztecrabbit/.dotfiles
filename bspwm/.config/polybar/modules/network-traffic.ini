[module/network-traffic]
type = custom/script
exec = ~/.config/polybar/modules/scripts/network-traffic.sh
tail = true

format = <label>
format-suffix = ${variables.network-traffic-format-suffix}
format-suffix-margin-left = ${variables.network-traffic-format-suffix-margin-left}
format-margin = ${variables.wireless-network-format-margin}

label = %output%
